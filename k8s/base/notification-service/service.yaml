apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: staging
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3003
  type: ClusterIP
