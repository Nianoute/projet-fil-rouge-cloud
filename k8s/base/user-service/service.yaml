apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: staging
spec:
  selector:
    app: user-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP