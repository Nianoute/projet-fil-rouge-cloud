apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: staging
spec:
  selector:
    app: task-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3002
  type: ClusterIP
